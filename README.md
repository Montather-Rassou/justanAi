# justanAi

A basic HR Management System (HRMS) example using HTML and JavaScript. It allows adding and removing employees with data stored in the browser's local storage.

Open `index.html` in a web browser to try it out.
